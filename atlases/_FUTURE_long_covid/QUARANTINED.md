# QUARANTINED — not active work

This Long COVID atlas seed was scaffolded as the first proof of the
multi-atlas platform substrate, but the project commitment is to **nail
the autism vertical before branching out**. This directory is preserved
as documented future direction; nothing here ships in the current
public surface (no UI loads it; the Streamlit app is autism-only).

When autism v1.0 is genuinely shipped (validated responder-rate cohort,
clinician adoption, preprint live), revisit and rename back to
`atlases/long_covid/v0.1/`.

Until then: **leave this alone**.
