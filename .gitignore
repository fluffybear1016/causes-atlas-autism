__pycache__/
*.pyc
*.pyo
.DS_Store
.vscode/
.idea/
*.swp
.env
.venv/
venv/
delta_squared_v1/rankings.csv
delta_squared_v1/components.csv
delta_squared_v1/score_history.csv
delta_squared_v1/anomaly_flags.csv
delta_squared_v1/reflexivity_audit.csv
delta_squared_v1/data_gaps.csv
delta_squared_v1/run_summary.json
delta_squared_v1/calibration_status.txt
delta_squared_v1/reflexivity_status.txt
.config/rclone/rclone.conf

# Copyrighted source PDFs — do not redistribute publicly
validation/responder_rate_calibration/papers/

# Obsidian UI state — churns on every vault open/close + every
# build_vault rebuild. Was causing recurring `.git/index.lock` conflicts
# and merge-resolution noise. The vault content stays under version
# control; only the workspace layout state is excluded.
vault/.obsidian/workspace.json
vault/.obsidian/workspace-mobile.json
vault/.obsidian/graph.json
